class Solution(object):
    def maxDepth(self, s):
        """
        :type s: str
        :rtype: int
        """
        count=0
        result=0
        for i in s: 
            if i == '(':
                count+=1 
                result=max(result,count)
            if i ==')':
                count-=1
        return result
